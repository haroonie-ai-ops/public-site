---
// THROWAWAY — see this branch's commit message. Deliberate ESLint-only
// violations, used once to prove the CI lint gate is real. Not merged.
const label = 'gate probe';
---

<img src="/favicon.svg" />
<a href="#">{label}</a>
